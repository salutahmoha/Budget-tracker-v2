// import {Router} from 'express';

// import {createBudget, getAllBudgets, getBudget, updateBudget, deleteBudget} from '../controllers/budget.controllers.js';
// const router = Router();

// // create a budget -/budgets
// router.post('', createBudget)

// // get all budgets - /budgets
// router.get('', getAllBudgets)

// // get a single budget - /budgets/:id
// router.get('/:title', getBudget)

// // update a budget - /budgets/:id
// router.patch('/:title', updateBudget)

// // delete a budget - /budgets/:id
// router.delete('/:title', deleteBudget)

// export default router;
